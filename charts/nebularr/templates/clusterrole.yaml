{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "nebularr.fullname" . }}-manager
  labels:
    {{- include "nebularr.labels" . | nindent 4 }}
rules:
  # Core resources - secrets for API keys
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - get
      - list
      - watch
  # Events - for health status reporting
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - create
      - patch
  # Arr CRDs - full management
  - apiGroups:
      - arr.rinzler.cloud
    resources:
      - lidarrconfigs
      - prowlarrconfigs
      - radarrconfigs
      - sonarrconfigs
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch
  # Arr CRDs - finalizers
  - apiGroups:
      - arr.rinzler.cloud
    resources:
      - lidarrconfigs/finalizers
      - prowlarrconfigs/finalizers
      - radarrconfigs/finalizers
      - sonarrconfigs/finalizers
    verbs:
      - update
  # Arr CRDs - status
  - apiGroups:
      - arr.rinzler.cloud
    resources:
      - lidarrconfigs/status
      - prowlarrconfigs/status
      - radarrconfigs/status
      - sonarrconfigs/status
    verbs:
      - get
      - patch
      - update
---
# Leader election role
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "nebularr.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nebularr.labels" . | nindent 4 }}
rules:
  - apiGroups:
      - ""
    resources:
      - configmaps
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - coordination.k8s.io
    resources:
      - leases
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - create
      - patch
{{- end }}
